# Scripts

Place repeatable agent helper scripts here when they become useful.

Script rules:

- Keep scripts self-contained.
- Include clear error messages.
- Avoid destructive filesystem operations.
- Prefer read-only audits unless the script name and documentation make mutation explicit.
